ui = true

backend "inmem" {}

storage "file" {
  path = "<%= @dataPath %>"
}

listener "tcp" {
  address     = "0.0.0.0:<%= @vaultPort %>"
  tls_disable = "true"
}

disable_mlock = "true"

api_addr = "http://127.0.0.1:<%= @vaultPort %>"
