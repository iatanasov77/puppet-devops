#----------------------------- Modules configuration --------------------------------
metricbeat.config.modules:
    path: ${path.config}/modules.d/*.yml
    reload.enabled: false

#----------------------------- Monitoring configuration --------------------------------
#- type: log

#----------------------------- Logstash output --------------------------------
output.logstash:
    hosts: ["<%= @config['host_name'] %>:<%= @config['logstash_port'] %>"]
