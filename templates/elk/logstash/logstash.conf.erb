input {
    beats {
        port => <%= @beatsPort %>
    }
}

output {
    elasticsearch {
        hosts   => ["http://localhost:<%= @elkPort %>"]
        index   => "%{[@metadata][beat]}-%{[@metadata][version]}-%{+YYYY.MM.dd}"
    }
}
